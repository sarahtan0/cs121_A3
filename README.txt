Create Index:
	- In the file index.py, modify line [209-210] to the path that you would like the partial indexes to save to (currently set to out own local paths)
	- Run the whole index.py file
		- Partial and final index, along with all supporting maps will be created

Run Search Engine:
	- Create the index
	- Run app.py and open the localhost link given